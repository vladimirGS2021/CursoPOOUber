<?
class Driver extends Account {
    public Driver(String name, String
    document, string email, string
    password){
        super(name, document, email,
        password);
      }
    void printDataDriver ())
        System. out .print1n("Document
        driver: " + document + " Name
        driver: " + name + " E-mail:"
        + email + "Password: "+
        password);
    }
}
?>